'use client';import{useState}from'react';type M={id:string;url:string;file_type:string};export default function ApprovedPostCard({post}:{post:{id:string;format:string;caption:string;media:M[];credits:string[]}}){const[i,setI]=useState(0),a=post.media[i],caption=post.caption.replace(/\n\nPhoto:\s*@?[^\n]+(?:\n|$)/gi,'').trim();return <article className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="flex items-center justify-between px-4 py-3"><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">✓ Approved</span><span className="text-xs text-stone-500">{post.format}{post.media.length>1?` · ${post.media.length} photos`:''}</span></div><div className="relative aspect-[4/5] bg-stone-100">{a?(a.file_type.startsWith('video/')?<video src={a.url} controls muted playsInline className="h-full w-full object-cover"/>:<img src={a.url} alt="" className="h-full w-full object-cover"/>):<div className="flex h-full items-center justify-center text-sm text-stone-400">Media unavailable</div>}{post.media.length>1&&<><button aria-label="Previous photo" onClick={()=>setI((i-1+post.media.length)%post.media.length)} className="absolute left-3 top-1/2 flex h-9 w-9 -translate-y-1/2 items-center justify-center rounded-full bg-black/55 text-xl text-white">‹</button><button aria-label="Next photo" onClick={()=>setI((i+1)%post.media.length)} className="absolute right-3 top-1/2 flex h-9 w-9 -translate-y-1/2 items-center justify-center rounded-full bg-black/55 text-xl text-white">›</button><span className="absolute bottom-3 right-3 rounded-full bg-black/60 px-2.5 py-1 text-xs text-white">{i+1} of {post.media.length}</span></>}</div><div className="p-4"><p className="whitespace-pre-line text-sm leading-6 text-stone-700">{caption}</p>{post.credits.length>0&&<p className="mt-3 text-xs text-stone-500">Photo: {post.credits.join(', ')}</p>}</div></article>}
