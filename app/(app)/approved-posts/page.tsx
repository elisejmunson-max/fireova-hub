import type{Metadata}from'next';import Link from'next/link';import{createClient}from'@/lib/supabase/server';import ApprovedPostCard from'./approved-post-card';export const metadata:Metadata={title:'Approved Posts'};export default async function ApprovedPostsPage(){const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();let posts:any[]=[],bank:any[]=[];if(user&&user.id!=='dev'){const[{data},{data:bankRows}]=await Promise.all([sb.from('posts').select('id,format,caption_option1,created_at').eq('user_id',user.id).eq('pillar','Approved Posts').order('created_at',{ascending:false}),sb.from('media_assets').select('id,storage_path,file_type,tags').eq('user_id',user.id).order('created_at',{ascending:false}).limit(250)]);bank=bankRows||[];if(data?.length){const ids=data.map((p:any)=>p.id),{data:links}=await sb.from('post_media').select('post_id,asset_id,display_order').in('post_id',ids).order('display_order',{ascending:true}),assetIds=[...new Set((links||[]).map((l:any)=>l.asset_id))],{data:assets}=assetIds.length?await sb.from('media_assets').select('id,storage_path,file_type,tags').in('id',assetIds).eq('user_id',user.id):{data:[]};posts=data.map((p:any)=>({...p,media:(links||[]).filter((l:any)=>l.post_id===p.id).map((l:any)=>assets?.find((a:any)=>a.id===l.asset_id)).filter(Boolean)}))}}const url=(a:any)=>sb.storage.from('media').getPublicUrl(a.storage_path).data.publicUrl,credit=(p:any)=>[...new Set(p.media.flatMap((a:any)=>(a.tags||[]).filter((t:string)=>t.startsWith('photographer:')).map((t:string)=>t.slice(13))))]as string[],bankMedia=bank.map((a:any)=>({id:a.id,file_type:a.file_type,url:url(a),credit:(a.tags||[]).find((t:string)=>t.startsWith('photographer:'))?.slice(13)||''}));return <div><div className="page-header"><div className="flex items-start justify-between"><div><p className="mb-1 text-xs text-stone-400">Fireova Content</p><h1 className="text-2xl font-semibold">Approved Posts</h1><p className="mt-1 text-sm text-stone-500">{posts.length} ready-to-use {posts.length===1?'post':'posts'} saved.</p></div><Link href="/dashboard" className="btn-primary">Review More</Link></div></div><div className="page-content">{posts.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{posts.map(p=><ApprovedPostCard key={p.id} bank={bankMedia} post={{id:p.id,format:p.format,caption:p.caption_option1||'',credits:credit(p),media:p.media.map((a:any)=>({id:a.id,file_type:a.file_type,url:url(a),credit:(a.tags||[]).find((t:string)=>t.startsWith('photographer:'))?.slice(13)||''}))}}/>)}</div>:<div className="card p-10 text-center"><h2 className="font-semibold">No approved posts yet</h2><p className="mt-1 text-sm text-stone-500">Approve posts you like and they'll collect here.</p><Link href="/dashboard" className="btn-primary mt-5">Review Posts</Link></div>}</div></div>}
