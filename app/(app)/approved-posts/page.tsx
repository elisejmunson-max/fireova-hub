import type{Metadata}from'next';import Link from'next/link';import{createClient}from'@/lib/supabase/server';import ApprovedPostsGrid from'./approved-posts-grid';export const metadata:Metadata={title:'Approved Posts'};export const dynamic='force-dynamic';export const revalidate=0;export default async function ApprovedPostsPage(){const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();let posts:any[]=[];if(user&&user.id!=='dev'){const{data}=await sb.from('posts').select('id,format,caption_option1,created_at,sort_order').eq('user_id',user.id).eq('pillar','Approved Posts').order('sort_order',{ascending:true,nullsFirst:false}).order('created_at',{ascending:false});if(data?.length){const ids=data.map((p:any)=>p.id),{data:links}=await sb.from('post_media').select('post_id,asset_id,display_order').in('post_id',ids).order('display_order',{ascending:true}),assetIds=[...new Set((links||[]).map((l:any)=>l.asset_id))],{data:assets}=assetIds.length?await sb.from('media_assets').select('id,storage_path,file_type,tags').in('id',assetIds).eq('user_id',user.id):{data:[]};posts=data.map((p:any)=>({...p,media:(links||[]).filter((l:any)=>l.post_id===p.id).map((l:any)=>assets?.find((a:any)=>a.id===l.asset_id)).filter(Boolean)}))}}const url=(a:any)=>sb.storage.from('media').getPublicUrl(a.storage_path).data.publicUrl,credit=(p:any)=>[...new Set(p.media.flatMap((a:any)=>(a.tags||[]).filter((t:string)=>t.startsWith('photographer:')).map((t:string)=>t.slice(13))))]as string[],gridPosts=posts.map(p=>({id:p.id,format:p.format,caption:String(p.caption_option1||'').replace(/\n\nPhoto:\s*@?[^\n]+(?:\n|$)/gi,'').trim(),credits:credit(p),sort_order:p.sort_order??null,media:p.media.map((a:any)=>({id:a.id,file_type:a.file_type,url:url(a),credit:(a.tags||[]).find((t:string)=>t.startsWith('photographer:'))?.slice(13)||''}))}));return <div><div className="page-header"><div className="flex items-start justify-between"><div><p className="mb-1 text-xs text-stone-400">Fireova Content</p><h1 className="text-2xl font-semibold">Approved Posts</h1><p className="mt-1 text-sm text-stone-500">Plan the feed before you post it.</p></div><Link href="/dashboard" className="btn-primary">Review More</Link></div></div><div className="page-content">{posts.length?<ApprovedPostsGrid initialPosts={gridPosts}/>:<div className="card p-10 text-center"><h2 className="font-semibold">No approved posts yet</h2><p className="mt-1 text-sm text-stone-500">Approve posts you like and they'll collect here.</p><Link href="/dashboard" className="btn-primary mt-5">Review Posts</Link></div>}</div></div>}
