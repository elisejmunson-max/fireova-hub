import{NextRequest}from'next/server';import{createClient}from'@/lib/supabase/server';function hashtags(caption:string){return caption.match(/#[A-Za-z0-9_]+/g)||[]}export async function POST(req:NextRequest){const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();if(!user)return Response.json({error:'Unauthorized'},{status:401});const{assetIds,caption,format,scheduledDate,day}=await req.json();if(!Array.isArray(assetIds)||!assetIds.length||!caption||!scheduledDate)return Response.json({error:'Missing approval details'},{status:400});const ids=[...new Set(assetIds.map(String))],{data:owned}=await sb.from('media_assets').select('id,storage_path').in('id',ids).eq('user_id',user.id);if((owned||[]).length!==ids.length)return Response.json({error:'Media not found'},{status:404});const cleanCaption=String(caption).trim(),tags=hashtags(cleanCaption),title=`This Week · ${day||scheduledDate}`,thumb=owned?.[0]?sb.storage.from('media').getPublicUrl(owned[0].storage_path).data.publicUrl:null,{data:post,error}=await sb.from('posts').insert({user_id:user.id,title,pillar:'This Week',topic:'Weekly social post',format,status:'scheduled',scheduled_date:scheduledDate,caption_option1:cleanCaption,caption_option2:null,caption_option3:null,hashtags:tags,shot_ideas:[],notes:'Approved from This Week',thumbnail_url:thumb,approved:true}).select('id').single();if(error||!post)return Response.json({error:error?.message||'Could not save approved post'},{status:500});const{error:mediaError}=await sb.from('post_media').insert(ids.map((assetId:string,i:number)=>({post_id:post.id,asset_id:assetId,display_order:i})));if(mediaError){await sb.from('posts').delete().eq('id',post.id).eq('user_id',user.id);return Response.json({error:mediaError.message||'Could not attach media'},{status:500})}return Response.json({ok:true,postId:post.id,scheduledDate})}
