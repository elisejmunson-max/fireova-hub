import{NextRequest}from'next/server';import{createClient}from'@/lib/supabase/server';function text(d:any){if(typeof d?.output_text==='string')return d.output_text.trim();for(const x of d?.output||[])for(const c of x?.content||[])if(c?.type==='output_text')return String(c.text||'').trim();return''}async function ask(key:string,input:any[],tokens=900){const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-5.6',reasoning:{effort:'medium'},input,max_output_tokens:tokens})}),d=await r.json();if(!r.ok)throw new Error(d?.error?.message||'OpenAI request failed');return text(d).replace(/^```json\s*/i,'').replace(/\s*```$/,'').trim()}const STRATEGIST=`You are Fireova's content strategist, not the caption writer. Analyze each supplied social post independently. Decide what the media MEANS and what is genuinely worth saying. Do not write captions or inventory every visible object. A wedding Reel can feature a funny detail and still be a wedding Reel. People doing their jobs are not automatically the story and employee names must never be used. Prefer a concrete angle over a brand lesson. CRITICAL VISUAL TRUTH RULE: for Photo and Carousel posts, only claim foods, objects, actions or people that are actually visible in the CURRENT images supplied in this request. Never introduce an item from old metadata or inference. If you cannot verify it visually, leave it out. Return only JSON {"posts":[{"story":"...","interesting_angle":"...","why_share":"...","tone":"...","avoid":"..."}]}.`;const WRITER=`You write Fireova Pizza Instagram captions from strategy already created by a thoughtful editor. Sound like the actual owner/team, not a professional copywriter. Voice is warm, conversational, easy, humble and occasionally playful. No em dash. Never say pie. No employee names. No robotic CTA. No hashtags by default. Max 2 emojis. Specific > sentimental. Interesting > inspirational. Casual > polished. Stop trying to sound profound. Do not narrate every image. CRITICAL: never add a food, object, action, person, venue detail or event fact that the strategy did not establish. Do not embellish. Return only JSON {"posts":[{"caption":"..."}]}.`;export async function POST(req:NextRequest){try{const key=process.env.OPENAI_API_KEY;if(!key)return Response.json({error:'OpenAI captioning is not configured yet.'},{status:500});const body=await req.json(),posts=body?.posts;if(!Array.isArray(posts)||posts.length<1||posts.length>3)return Response.json({error:'One to three posts required'},{status:400});const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();if(!user)return Response.json({error:'Unauthorized'},{status:401});const ids=[...new Set(posts.flatMap((p:any)=>Array.isArray(p.assetIds)?p.assetIds:[]).map(String))];const[{data:rows},{data:recent}]=await Promise.all([sb.from('media_assets').select('id,storage_path,file_type,ai_reason,tags').in('id',ids).eq('user_id',user.id),sb.from('posts').select('caption_option1,caption_option2,notes').eq('user_id',user.id).eq('pillar','Approved Posts').order('created_at',{ascending:false}).limit(30)]),byId=new Map((rows||[]).map((a:any)=>[a.id,a])),strategyInput:any[]=[{role:'system',content:[{type:'input_text',text:STRATEGIST}]}];for(let i=0;i<posts.length;i++){const p=posts[i],media=(p.assetIds||[]).map((id:string)=>byId.get(id)).filter(Boolean)as any[],isReel=String(p.format).toLowerCase()==='reel',events=[...new Set(media.flatMap((a:any)=>(a.tags||[]).filter((t:string)=>/^(event:|planning:)/.test(t)).map((t:string)=>t.replace(/^(event:|planning:)/,''))))],context=isReel?`Stored WHOLE-VIDEO context (useful because the video itself is not sent): ${media.map((a:any)=>a.ai_reason).filter(Boolean).join(' | ')||'none'}. Event hints: ${events.join(', ')||'none'}.`:`CURRENT ${p.format}: judge ONLY the images attached below. Ignore stored AI descriptions and subject/food tags; they may be stale or wrong. Event hints may establish broad event type only: ${events.join(', ')||'none'}.`;const content:any[]=[{type:'input_text',text:`POST ${i+1}. Format: ${p.format}. ${context} Employee identities are withheld.`}];for(const a of media.slice(0,5))if(String(a.file_type||'').startsWith('image/'))content.push({type:'input_image',image_url:sb.storage.from('media').getPublicUrl(a.storage_path).data.publicUrl,detail:'auto'});strategyInput.push({role:'user',content})}strategyInput.push({role:'user',content:[{type:'input_text',text:`Analyze all ${posts.length} post${posts.length===1?'':'s'}. For photos/carousels, verify every concrete claim against the current images. Raw JSON only.`}]});const strategy=JSON.parse(await ask(key,strategyInput,1000));if(!Array.isArray(strategy.posts)||strategy.posts.length!==posts.length)throw new Error('Invalid content strategy');const recentCaptions=(recent||[]).map((x:any)=>String(x.caption_option1||'').trim()).filter(Boolean),edits=(recent||[]).filter((x:any)=>String(x.notes||'').startsWith('Approved Post Bank')&&x.caption_option1&&x.caption_option2&&x.caption_option1!==x.caption_option2).slice(0,12).map((x:any)=>`AI: ${x.caption_option2}\nFINAL: ${x.caption_option1}`).join('\n---\n'),writerInput:any[]=[{role:'system',content:[{type:'input_text',text:WRITER}]},{role:'user',content:[{type:'input_text',text:`CONTENT STRATEGY:\n${JSON.stringify(strategy.posts,null,2)}\n\nRECENT APPROVED CAPTIONS TO AVOID REPEATING:\n${recentCaptions.length?recentCaptions.join('\n'):'None yet.'}${edits?`\n\nUSER EDIT HISTORY:\n${edits}`:''}\n\nWrite ${posts.length} caption${posts.length===1?'':'s'} in order. Raw JSON only.`}]}];const parsed=JSON.parse(await ask(key,writerInput,900));if(!Array.isArray(parsed.posts)||parsed.posts.length!==posts.length)throw new Error('Invalid captions returned');return Response.json({posts:parsed.posts.map((x:any)=>({caption:String(x.caption||'').trim()}))})}catch(e){return Response.json({error:e instanceof Error?e.message:'Could not create review set'},{status:500})}}
