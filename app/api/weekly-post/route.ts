import Anthropic from '@anthropic-ai/sdk'
import{NextRequest}from'next/server';import{createClient}from'@/lib/supabase/server';
const SYSTEM=`You create finished Instagram posts for Fireova Pizza, a DFW mobile wood-fired catering company. Write in Fireova's real team voice: warm, conversational, short, grounded, never corporate. Use we/our/us. No em dashes. Never say pie. Never use elevate, curated, seamless, unforgettable, game-changer, perfect for, or dream event. Do not invent names, venue details, or facts that are not provided. Caption target is 1-3 short sentences, maximum 4. Use 1-3 natural emojis. Exactly 4 relevant hashtags. Fireova can feature weddings, corporate events, private parties, wood-fired pizza, small bites, charcuterie, team, guest experience, and onsite cooking. The post must feel ready to publish, not like AI analysis. Return ONLY JSON: {"caption":"finished caption including hashtags","title":"short internal title"}.`;
export async function POST(req:NextRequest){const key=process.env.ANTHROPIC_API_KEY;if(!key)return Response.json({error:'AI is not configured.'},{status:500});const{assetId,day,format}=await req.json();const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();if(!user)return Response.json({error:'Unauthorized'},{status:401});const{data:a}=await sb.from('media_assets').select('id,filename,storage_path,file_type,ai_reason,ai_categories,ai_post_uses,tags').eq('id',assetId).eq('user_id',user.id).single();if(!a)return Response.json({error:'Media not found'},{status:404});const url=sb.storage.from('media').getPublicUrl(a.storage_path).data.publicUrl;const parts:any[]=[];if(String(a.file_type||'').startsWith('image/'))parts.push({type:'image',source:{type:'url',url}});parts.push({type:'text',text:`Create the finished ${day} ${format} post. Media review context: ${a.ai_reason||'none'}. Categories: ${(a.ai_categories||[]).join(', ')}. Possible uses: ${(a.ai_post_uses||[]).join(', ')}. Do not repeat this analysis language. Turn the real visual/content into a natural Fireova social caption. Return raw JSON only.`});try{const client=new Anthropic({apiKey:key}),m=await client.messages.create({model:'claude-sonnet-4-5',max_tokens:600,system:SYSTEM,messages:[{role:'user',content:parts}]}),b=m.content.find(x=>x.type==='text');if(!b||b.type!=='text')throw new Error('No caption returned');return new Response(b.text.replace(/^```json\s*/i,'').replace(/\s*```$/,'').trim(),{headers:{'Content-Type':'application/json'}})}catch(e){return Response.json({error:e instanceof Error?e.message:'Could not create post'},{status:500})}}
