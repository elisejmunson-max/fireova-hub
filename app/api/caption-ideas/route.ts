import{NextRequest}from'next/server';import{createClient}from'@/lib/supabase/server';import{FIREOVA_VOICE_EXAMPLES,FIREOVA_VOICE_RULES}from'@/lib/fireova-voice';
const text=(d:any)=>d?.output_text||d?.output?.flatMap((x:any)=>x.content||[]).find((x:any)=>x.type==='output_text')?.text||'';
function parse(s:string){const c=s.trim().replace(/^```json\\s*/i,'').replace(/```$/,'').trim();try{return JSON.parse(c)}catch{const a=c.indexOf('{'),b=c.lastIndexOf('}');if(a>=0&&b>a)return JSON.parse(c.slice(a,b+1));throw new Error('Could not finish caption ideas')}}
export async function POST(req:NextRequest){try{const key=process.env.OPENAI_API_KEY;if(!key)return Response.json({error:'AI captioning is not configured.'},{status:500});const{request}=await req.json();const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();if(!user)return Response.json({error:'Unauthorized'},{status:401});if(!String(request||'').trim())return Response.json({error:'Tell Fireova what you want to post about.'},{status:400});const system=`You are Fireova Pizza's senior social writer. The owner supplies topics. Do NOT choose media yet. Parse the owner's request into up to 3 literal topics. For EACH topic, silently develop 3 genuinely different POST ANGLES first, then write exactly one caption from each angle. The three captions must represent different thoughts, not three rewrites of the same observation. Do not substitute topics. These are caption ideas first; media will be found only after the owner approves a caption. ${FIREOVA_VOICE_RULES}\nApproved voice examples:\n${FIREOVA_VOICE_EXAMPLES.join('\\n')}\nDo not output the hidden angles, only the finished captions. Avoid generic labels, alt text, manufactured poetry, marketing copy, and empty cleverness. Reject placeholder-style lines such as 'Fireova style,' 'a closer look at,' 'the team behind,' 'we loved being part of,' or captions that merely restate the topic. Each option should contain a real human thought that could stand on its own even before media is selected. Do not use employee names unless the topic explicitly asks for a named person. For a holiday/promo topic, only state a specific holiday/date/promotion if the owner supplied it; otherwise keep the idea honest rather than inventing details. Return JSON only: {"ideas":[{"topic":"literal topic","options":[{"caption":"...","mediaId":"candidate id if this caption was inspired by a supplied image, otherwise blank"}]}]}`;const{data:pref}=await sb.from('review_feedback').select('action,ai_caption,metadata,created_at').eq('user_id',user.id).in('action',['caption_idea_liked','caption_idea_chosen']).order('created_at',{ascending:false}).limit(40);const preferenceNote=(pref||[]).map((p:any)=>`${p.metadata?.topic||'topic'}: ${p.ai_caption}`).join('\n');const topics=String(request).split(',').map(x=>x.trim()).filter(Boolean).slice(0,3),broad=/^(wedding|team|event|behind the scenes|people|guests?)$/i,visuals:any[]=[];for(const topic of topics){if(!broad.test(topic))continue;const{data}=await sb.from('media_assets').select('id,storage_path,file_type,tags,ai_reason').eq('user_id',user.id).order('created_at',{ascending:false}).limit(150);const words=topic.toLowerCase().split(/\s+/),ranked=(data||[]).filter((a:any)=>a.file_type?.startsWith('image/')).map((a:any)=>{const s=((a.tags||[]).join(' ')+' '+(a.ai_reason||'')).toLowerCase();return{a,score:words.reduce((n,w)=>n+(s.includes(w)?1:0),0)}}).sort((a:any,b:any)=>b.score-a.score).slice(0,4);if(ranked.length)visuals.push({topic,items:ranked.map((x:any)=>({id:x.a.id,url:sb.storage.from('media').getPublicUrl(x.a.storage_path).data.publicUrl,tags:x.a.tags||[],reason:x.a.ai_reason||''}))})}const visualNote=visuals.length?'\nFor broad topics, use the supplied candidate images to ground captions in a real moment. Do not mention details you cannot verify.':'';const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:'Bearer '+key,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-5.6-sol',reasoning:{effort:'low'},max_output_tokens:2400,input:[{role:'system',content:[{type:'input_text',text:system+visualNote+(preferenceNote?'\nPreviously liked/chosen caption ideas. Treat these as positive creative memory; learn the angle and voice without mechanically repeating them:\n'+preferenceNote:'')}]},{role:'user',content:[{type:'input_text',text:String(request)},{type:'input_text',text:'Broad-topic visual candidates: '+JSON.stringify(visuals.map(v=>({topic:v.topic,items:v.items.map((i:any)=>({id:i.id,tags:i.tags,reason:i.reason}))})))},...visuals.flatMap(v=>v.items.map((i:any)=>({type:'input_image',image_url:i.url,detail:'low'})))]}]})}),d=await r.json();if(!r.ok)throw new Error(d?.error?.message||'Could not create caption ideas');let out:any;try{out=parse(text(d))}catch{const retry=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:'Bearer '+key,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-5.6-sol',reasoning:{effort:'low'},max_output_tokens:2400,input:[{role:'system',content:[{type:'input_text',text:system+' Keep the JSON compact and complete.'}]},{role:'user',content:[{type:'input_text',text:String(request)}]}]})}),rd=await retry.json();if(!retry.ok)throw new Error(rd?.error?.message||'Could not create caption ideas');out=parse(text(rd))}if(!Array.isArray(out.ideas))throw new Error('Could not finish caption ideas');const byId=new Map(visuals.flatMap(v=>v.items).map((i:any)=>[i.id,i]));const ideas=out.ideas.slice(0,3).map((idea:any)=>({topic:idea.topic,options:(idea.options||idea.captions?.map((caption:string)=>({caption,mediaId:''}))||[]).slice(0,3).map((o:any)=>({caption:String(o.caption||''),mediaId:String(o.mediaId||''),mediaUrl:byId.get(String(o.mediaId||''))?.url||''}))}));return Response.json({ok:true,ideas})}catch(e){return Response.json({error:e instanceof Error?e.message:'Could not create caption ideas'},{status:500})}}