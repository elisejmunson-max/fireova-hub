import Anthropic from'@anthropic-ai/sdk';import{NextRequest,NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';export const runtime='nodejs';const SYSTEM=`Identify ONLY literal things physically visible in this Fireova image for Media Bank object search. Return ONLY JSON: {"subjects":["specific visible subject"],"categories":["broad planning category"],"description":"one short factual description"}. IMPORTANT: words, logos, business names, signage, printed menus, shirt text, tent text, oven branding, captions, and other written text DO NOT count as the object named by that text. If the words "Fireova Pizza" appear on a shirt/tent/oven but no actual pizza food is visible, DO NOT include pizza. Tag pizza ONLY when actual edible pizza or pizza slices are visibly present. Tag pizza oven when an oven is visibly present. Tag team/person only when people are visibly present. Examples: prosciutto wrapped shrimp, shrimp platter, caesar salad, whole pizza, pizza slice, wood-fired pizza, pizza oven, charcuterie board, tiramisu, team photo. Do not infer event type, ingredients, or context unless directly visible. Use literal synonyms only when they refer to the same visible physical thing.`;export async function POST(req:NextRequest){try{const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({ok:false},{status:401});const{assetId}=await req.json(),{data:a}=await sb.from('media_assets').select('*').eq('id',assetId).eq('user_id',user.id).single();if(!a||!a.file_type?.startsWith('image/'))return NextResponse.json({ok:false});const key=process.env.ANTHROPIC_API_KEY;if(!key)return NextResponse.json({ok:false});const u=sb.storage.from('media').getPublicUrl(a.storage_path).data.publicUrl,m=await new Anthropic({apiKey:key}).messages.create({model:'claude-sonnet-4-5',max_tokens:450,system:SYSTEM,messages:[{role:'user',content:[{type:'image',source:{type:'url',url:u}},{type:'text',text:'Create literal visible-subject search tags. Ignore all written words/logos as evidence of objects. Raw JSON only.'}]}]}),b=m.content.find(x=>x.type==='text');if(!b||b.type!=='text')return NextResponse.json({ok:false});const p=JSON.parse(b.text.replace(/^```json\s*/i,'').replace(/\s*```$/,'').trim()),subjects=Array.isArray(p.subjects)?p.subjects.map((x:any)=>String(x).toLowerCase().trim()).filter(Boolean).slice(0,15):[],cats=Array.isArray(p.categories)?p.categories.map((x:any)=>String(x)).slice(0,8):[];const preserved=Array.isArray(a.tags)?a.tags.filter((x:any)=>/^(event:|planning:|person:|photographer:)/.test(String(x))):[];const tags=Array.from(new Set([...preserved,...subjects.map((x:string)=>`subject:${x}`)])),patch={tags,ai_categories:cats,ai_reason:String(p.description||''),ai_reviewed_at:new Date().toISOString()};await sb.from('media_assets').update(patch).eq('id',a.id).eq('user_id',user.id);return NextResponse.json({ok:true,...patch,subjects})}catch{return NextResponse.json({ok:false})}}
