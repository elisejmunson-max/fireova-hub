import Anthropic from'@anthropic-ai/sdk';import{NextRequest,NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';export const runtime='nodejs';const SYSTEM=`Identify ONLY what is visibly shown in this Fireova Pizza image for literal Media Bank search. Return ONLY JSON: {"subjects":["specific searchable phrase"],"categories":["broad planning category"],"description":"one short factual description"}. SUBJECTS must describe visible objects/food/people only. Examples: prosciutto wrapped shrimp, shrimp platter, caesar salad, pizza slice, wood-fired pizza, pizza oven, charcuterie table, tiramisu, team photo, wedding menu. Do NOT add pizza just because Fireova is a pizza company. Do NOT infer wedding, corporate, venue, catering, event type, ingredients, or context unless directly visible. Include useful literal synonyms only when they describe the same visible thing.`;export async function POST(req:NextRequest){try{const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({ok:false},{status:401});const{assetId}=await req.json(),{data:a}=await sb.from('media_assets').select('*').eq('id',assetId).eq('user_id',user.id).single();if(!a||!a.file_type?.startsWith('image/'))return NextResponse.json({ok:false});const key=process.env.ANTHROPIC_API_KEY;if(!key)return NextResponse.json({ok:false});const u=sb.storage.from('media').getPublicUrl(a.storage_path).data.publicUrl,m=await new Anthropic({apiKey:key}).messages.create({model:'claude-sonnet-4-5',max_tokens:450,system:SYSTEM,messages:[{role:'user',content:[{type:'image',source:{type:'url',url:u}},{type:'text',text:'Create literal visible-subject search tags. Raw JSON only.'}]}]}),b=m.content.find(x=>x.type==='text');if(!b||b.type!=='text')return NextResponse.json({ok:false});const p=JSON.parse(b.text.replace(/^```json\s*/i,'').replace(/\s*```$/,'').trim()),subjects=Array.isArray(p.subjects)?p.subjects.map((x:any)=>String(x).toLowerCase().trim()).filter(Boolean).slice(0,15):[],cats=Array.isArray(p.categories)?p.categories.map((x:any)=>String(x)).slice(0,8):[];const planning=Array.isArray(a.tags)?a.tags.filter((x:any)=>String(x).startsWith('event:')||String(x).startsWith('planning:')):[];const searchTags=subjects.map((x:string)=>`subject:${x}`),tags=Array.from(new Set([...planning,...searchTags]));const patch={tags,ai_categories:cats,ai_reason:String(p.description||''),ai_reviewed_at:new Date().toISOString()};await sb.from('media_assets').update(patch).eq('id',a.id).eq('user_id',user.id);return NextResponse.json({ok:true,...patch,subjects})}catch{return NextResponse.json({ok:false})}}
