import Anthropic from'@anthropic-ai/sdk';import{NextRequest,NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';export const runtime='nodejs';const SYSTEM=`Identify what is visibly shown in this Fireova Pizza media asset so it can be found later by search. Return ONLY JSON: {"subjects":["specific searchable phrase"],"categories":["broad category"],"description":"one short factual description"}. Use specific terms such as prosciutto wrapped shrimp, charcuterie table, grazing table, wood-fired pizza, pizza oven, team photo, wedding menu, tiramisu, cannoli, caprese skewers when actually visible. Add useful broader synonyms such as shrimp appetizer, small bites, appetizers, wedding, team, dessert. Never invent ingredients or event facts.`;export async function POST(req:NextRequest){try{const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({ok:false},{status:401});const{assetId}=await req.json(),{data:a}=await sb.from('media_assets').select('*').eq('id',assetId).eq('user_id',user.id).single();if(!a||!a.file_type?.startsWith('image/'))return NextResponse.json({ok:false});const key=process.env.ANTHROPIC_API_KEY;if(!key)return NextResponse.json({ok:false});const u=sb.storage.from('media').getPublicUrl(a.storage_path).data.publicUrl,m=await new Anthropic({apiKey:key}).messages.create({model:'claude-sonnet-4-5',max_tokens:450,system:SYSTEM,messages:[{role:'user',content:[{type:'image',source:{type:'url',url:u}},{type:'text',text:'Tag this asset for Media Bank search. Return raw JSON only.'}]}]}),b=m.content.find(x=>x.type==='text');if(!b||b.type!=='text')return NextResponse.json({ok:false});const p=JSON.parse(b.text.replace(/^```json\s*/i,'').replace(/\s*```$/,'').trim()),subjects=Array.isArray(p.subjects)?p.subjects.map((x:any)=>String(x).toLowerCase().trim()).filter(Boolean):[],cats=Array.isArray(p.categories)?p.categories.map((x:any)=>String(x)):[],existing=Array.isArray(a.tags)?a.tags:[],tags=Array.from(new Set([...existing,...subjects,...cats.map((x:string)=>x.toLowerCase())]));const patch={tags,ai_categories:cats,ai_reason:String(p.description||''),ai_reviewed_at:new Date().toISOString()};await sb.from('media_assets').update(patch).eq('id',a.id).eq('user_id',user.id);return NextResponse.json({ok:true,...patch})}catch{return NextResponse.json({ok:false})}}
