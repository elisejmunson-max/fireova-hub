import Anthropic from'@anthropic-ai/sdk';import{NextRequest}from'next/server';import{createClient}from'@/lib/supabase/server';const SYSTEM=`Analyze sampled frames from one Fireova video for literal Media Bank search. Identify ONLY things visibly shown across the video. Return ONLY raw JSON: {"subjects":["specific searchable phrase"],"categories":["broad planning category"],"description":"short factual description"}. SUBJECTS are literal visible objects, foods, people or actions, e.g. wood-fired pizza, pizza oven, team serving pizza, charcuterie table, grazing table, tiramisu, event menu, people making pizza. Do NOT tag pizza merely because Fireova is a pizza company. Do NOT infer event type or ingredients unless clearly visible. Use 3-15 useful literal phrases/synonyms.`;export async function POST(req:NextRequest){try{const sb=createClient()as any,{data:{user}}=await sb.auth.getUser();if(!user)return Response.json({error:'Unauthorized'},{status:401});const key=process.env.ANTHROPIC_API_KEY;if(!key)return Response.json({error:'AI not configured'},{status:500});const body=await req.json()as{assetId?:string;frames?:string[]};if(!body.assetId||!body.frames?.length)return Response.json({error:'assetId and frames required'},{status:400});const{data:a}=await sb.from('media_assets').select('*').eq('id',body.assetId).eq('user_id',user.id).single();if(!a||!a.file_type?.startsWith('video/'))return Response.json({error:'Video not found'},{status:404});const content:any[]=body.frames.slice(0,6).map(f=>({type:'image',source:{type:'base64',media_type:'image/jpeg',data:f.replace(/^data:image\/jpeg;base64,/,'')}}));content.push({type:'text',text:'Create literal visible-subject search tags for this video. Raw JSON only.'});const msg=await new Anthropic({apiKey:key}).messages.create({model:'claude-sonnet-4-5',max_tokens:500,system:SYSTEM,messages:[{role:'user',content}]}),b=msg.content.find(x=>x.type==='text'),raw=b&&b.type==='text'?b.text.trim():'';const p=JSON.parse(raw.replace(/^```json\s*/i,'').replace(/\s*```$/,'').trim()),subjects=Array.isArray(p.subjects)?p.subjects.map((x:any)=>String(x).toLowerCase().trim()).filter(Boolean).slice(0,15):[],cats=Array.isArray(p.categories)?p.categories.map((x:any)=>String(x)).slice(0,8):[],planning=Array.isArray(a.tags)?a.tags.filter((x:any)=>String(x).startsWith('event:')||String(x).startsWith('planning:')):[],tags=Array.from(new Set([...planning,...subjects.map((x:string)=>`subject:${x}`)])),patch={tags,ai_categories:cats,ai_reason:String(p.description||''),ai_reviewed_at:new Date().toISOString()};await sb.from('media_assets').update(patch).eq('id',a.id).eq('user_id',user.id);return Response.json({ok:true,id:a.id,...patch})}catch(e){return Response.json({error:e instanceof Error?e.message:'Video analysis failed'},{status:500})}}
